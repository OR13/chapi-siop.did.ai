import React from "react";
import PropTypes from "prop-types";

import Base from "../../base/base";

export const ChapiWindowGet = (props) => {
  return <Base>ChapiWindowGet</Base>;
};

ChapiWindowGet.propTypes = {
  wallet: PropTypes.any,
};
