import React from "react";
import PropTypes from "prop-types";

import Base from "../../base/base";

export const ChapiWindowStore = (props) => {
  return <Base>ChapiWindowStore</Base>;
};

ChapiWindowStore.propTypes = {
  wallet: PropTypes.any,
};
