import React from "react";
import PropTypes from "prop-types";

import Base from "../base/base";

export const Verifier = (props) => {
  return <Base>Verifier</Base>;
};

Verifier.propTypes = {
  wallet: PropTypes.any,
};
